[package]
name = "risc0-private-tx"
version = "0.1.0"
edition = "2024"

[dependencies]
private-tx-guest = { path = "../../../utils/zkvm/private-tx-guest" }
risc0-zkvm = { version = "3.0.5", default-features = false, features = [
    "std",
    "unstable",
] }

[workspace]
